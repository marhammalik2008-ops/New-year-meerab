<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Happy New Year Meerab ❤️</title>

<style>
body {
    margin: 0;
    font-family: 'Georgia', serif;
    background: radial-gradient(circle at top, #1a1a2e, #0f3460);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    overflow: hidden;
    text-align: center;
}

.container {
    background: rgba(255,255,255,0.08);
    padding: 40px;
    border-radius: 25px;
    max-width: 460px;
    backdrop-filter: blur(10px);
    box-shadow: 0 0 45px rgba(233,69,96,0.6);
    z-index: 2;
}

h1 {
    color: #ff4d6d;
    font-size: 3rem;
    text-shadow: 0 0 20px rgba(255,77,109,0.7);
}

p {
    font-size: 1.15rem;
    line-height: 1.7;
}

.countdown {
    margin: 25px 0;
    font-size: 1.5rem;
    color: gold;
}

button {
    background: #ff4d6d;
    border: none;
    padding: 15px 35px;
    font-size: 1.1rem;
    border-radius: 40px;
    color: white;
    cursor: pointer;
    transition: 0.3s;
    box-shadow: 0 6px 20px rgba(255,77,109,0.6);
}

button:hover {
    transform: scale(1.08);
    background: #ff6f91;
}

.love-note {
    display: none;
    margin-top: 30px;
    animation: fadeIn 2s ease;
}

.sparkle {
    position: absolute;
    width: 6px;
    height: 6px;
    background: gold;
    border-radius: 50%;
    animation: floatUp 3s infinite;
}

.heart {
    position: absolute;
    font-size: 20px;
    color: #ff4d6d;
    animation: hearts 5s linear infinite;
}

@keyframes fadeIn {
    from {opacity:0; transform: translateY(20px);}
    to {opacity:1; transform: translateY(0);}
}

@keyframes floatUp {
    from {transform: translateY(110vh); opacity:1;}
    to {transform: translateY(-10vh); opacity:0;}
}

@keyframes hearts {
    from {transform: translateY(110vh);}
    to {transform: translateY(-10vh); opacity:0;}
}
</style>
</head>

<body>

<div class="container">
    <h1>Happy New Year ❤️</h1>
    <p>Another year begins… and I’m grateful it begins with you.</p>

    <div class="countdown" id="timer">Loading...</div>

    <button onclick="reveal()">Click for a Surprise 💌</button>

    <div class="love-note" id="note">
        <h2>My Love, Meerab 💖</h2>
        <p>
            Tomorrow marks <strong>5 beautiful months</strong> of us —
            five months of love I’ll always cherish.
        </p>
        <p>
            You are cute, angelic, heavenly beautiful,
            effortlessly sexy and absolutely perfect to me.
        </p>
        <p>
            I can’t wait to step into 2026 loving you even more. ✨
        </p>
        <p><strong>Forever yours ❤️</strong></p>
    </div>
</div>

<div class="sparkle" style="left:10%"></div>
<div class="sparkle" style="left:30%; animation-delay:1s"></div>
<div class="sparkle" style="left:60%; animation-delay:2s"></div>
<div class="sparkle" style="left:85%; animation-delay:.5s"></div>

<div class="heart" style="left:20%">❤️</div>
<div class="heart" style="left:50%">💖</div>
<div class="heart" style="left:75%">💕</div>

<script>
const target = new Date("Jan 1, 2026 00:00:00").getTime();

setInterval(() => {
    const now = new Date().getTime();
    const diff = target - now;

    if (diff < 0) {
        document.getElementById("timer").innerHTML = "🎆 HAPPY NEW YEAR MEERAB! 🎆";
        return;
    }

    const d = Math.floor(diff / (1000*60*60*24));
    const h = Math.floor((diff%(1000*60*60*24))/(1000*60*60));
    const m = Math.floor((diff%(1000*60*60))/(1000*60));
    const s = Math.floor((diff%(1000*60))/1000);

    document.getElementById("timer").innerHTML =
        `${d}d ${h}h ${m}m ${s}s`;
}, 1000);

function reveal() {
    document.getElementById("note").style.display = "block";
}
</script>

</body>
</html>
